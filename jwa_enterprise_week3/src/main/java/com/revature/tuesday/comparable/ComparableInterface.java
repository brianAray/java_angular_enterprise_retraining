package com.revature.tuesday.comparable;

public class ComparableInterface {
    /*
    Comparable Interface
    This interface is used to order the objects of the user defined class
    It only has one method, compareTo(Object)
    It provides a single sorting sequence only, you can sort the elements on the basis of single data member only

    the method
    compareTo(Object obj)

    Used to compare the current object with the specified object, it returns

    Postive integer if current object is greater
    negative if less
    zero if the same
     */
}
